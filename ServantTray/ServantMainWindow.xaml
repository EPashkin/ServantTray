<Window x:Class="ServantTray.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ServantTray"
        xmlns:cmd="clr-namespace:WPFUtil.Commands"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="Servant" Height="350" Width="525">
    <Grid>
        <tb:TaskbarIcon
            x:Name="TaskIcon"
            IconSource="/Icons/Bulb.ico"
            ToolTipText="Servant">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <ContextMenu.CommandBindings>
                        <cmd:DataContextCommandBinding Command="local:CustomCommands.Exit"
                            Executed="OnExit" CanExecute="ExitCanExecute" />
                    </ContextMenu.CommandBindings>
                    <MenuItem Header="First Menu Item" />
                    <MenuItem Header="Second Menu Item" />
                    <Separator/>
                    <MenuItem Header="{Binding ExitTitle}" Command="{Binding ExitCommand}" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

    </Grid>
</Window>
